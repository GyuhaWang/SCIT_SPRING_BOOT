<html xmlns:th="http://thymeleaf.org">
<body>
	<header th:fragment="header">
		<div class="header_section">
			<a th:href="@{/}" class="header_button home_button">home</a>
			<a th:if="${session.user_session != null}" 
		   th:href="@{/member/create}"  class="header_button">create</a>
			<a th:if="${session.user_session != null}" 
		   th:href="@{/member/users}"  class="header_button">read</a>
		   <a th:if="${session.user_session != null}" 
		   th:href="@{/member/update}"  class="header_button">update</a>
		   <a th:if="${session.user_session != null}" 
		   th:href="@{/member/delete}"  class="header_button">delete</a>
		</div>
	
		<div class="header_section">
			<a th:if="${session.user_session == null}" 
		   th:href="@{/member/join}"  class="header_button">signin</a>
			<a th:if="${session.user_session == null}" 
		   th:href="@{/member/login}"  class="header_button">login</a>
			<p th:if="${session.user_session != null}"
		   th:text="|welcome ${session.user_session.name}|" class="header_button"></p>
			<a th:if="${session.user_session != null}" 
		   th:href="@{/member/logout}"  class="header_button logout_button">logout</a>
		</div>
	</header>
</body>
</html>