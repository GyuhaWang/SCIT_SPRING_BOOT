<html xmlns:th="http://thymeleaf.org">
<body>
	<header th:fragment="header">
		<a th:href="@{/}" class="header_button">home</a>
		<div id="header_user_section">
		<a th:if="${session.user_session == null}" 
		   th:href="@{/member/join}"  class="header_button">signin</a>
		<a th:if="${session.user_session == null}" 
		   th:href="@{/member/login}"  class="header_button">login</a>
		<a th:if="${session.user_session != null}" 
		   th:href="@{/member/logout}"  class="header_button">logout</a>
		
		</div>
	</header>
</body>
</html>